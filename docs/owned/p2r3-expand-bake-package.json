{
  "$comment": "Invariant ids the P2 round-3 task `p2r3/expand-bake-package` turns green in tools/coursekit (G7 expand, G8 bake, G9 package). `owned` IS DELIBERATELY EMPTY AND MUST STAY EMPTY until `pnpm test:coverage-map` can read pytest — the same reason docs/owned/p2-g7.json, p2-g8.json, p2-g9.json and p2-deps-scaffold.json carry `owned: []`: packages/core/src/journey/ownership.ts reads only the `owned` array, requires the id to be in the P1 roster, requires a VITEST test whose name carries the id in brackets, and requires a falsifier at packages/core/src/<module>/__falsifiers__/<id>.json. None of the three holds for a P2 pytest lane. The ids this lane actually turns green are in `ownedByPytest`.",
  "task": "p2r3-expand-bake-package",
  "module": "tools/coursekit/src/coursekit (stages/g7_expand.py, stages/g8_bake.py, exercises/, tts/cast.py, packbuild/manifest.py, validators/exercise.py, validators/audio.py, config/g7.py, config/g8.py, config/base.py)",
  "phase": "P2",
  "screens": [
    "S032",
    "S033",
    "S034",
    "S035",
    "S036",
    "S037"
  ],
  "owned": [],
  "ownedByPytest": [
    "INV-PACK-02",
    "INV-PACK-07",
    "INV-PACK-15",
    "INV-PACK-17",
    "INV-PACK-41",
    "INV-PACK-50",
    "INV-PACK-51",
    "INV-AUD-08"
  ],
  "owningTests": {
    "INV-PACK-02": "tools/coursekit/tests/test_manifest.py::test_INV_PACK_02_item_ids_are_content_hashes_of_the_semantic_fields (new here — the id had NO test carrying it before this lane) + tests/test_itemid.py's INV-PACK-41 pair, which is the same hash from the other side",
    "INV-PACK-07": "tests/test_g7_expand.py::test_every_form_plan_row_carries_two_punitive_shapes (now over LEXEME_FORM_PLAN + LEXEME_MATCH_SHAPE), ::test_INV_PACK_07_the_written_pack_has_two_forms_for_every_missable_item, ::test_INV_PACK_07_a_match_is_only_built_over_lemmas_already_taught_twice, ::test_INV_PACK_07_a_word_or_fixed_phrase_is_a_lexeme_item_not_a_sentence_one (new) + five tests in tests/test_validators_exercise.py driven by tests/falsifiers/INV-PACK-07.json",
    "INV-PACK-15": "tests/test_g8_bake.py (four `inv_pack_15` tests) + tests/test_validators_audio.py (three) + tests/test_manifest.py::test_the_audio_budget_covers_all_three_pipelines",
    "INV-PACK-17": "tests/test_attribution.py (five) + tests/test_g9_package.py::test_INV_PACK_17_fails_the_stage_and_writes_nothing + tests/test_validators_freelingo.py (four F3 tests)",
    "INV-PACK-41": "tests/test_itemid.py::test_INV_PACK_41_falsifier and ::test_INV_PACK_41_holds_through_the_real_writer — unchanged by this lane and re-run against the regenerated fixture, which is the point: `accentClaim` and the distractor set are presentation fields and moved no item id",
    "INV-PACK-50": "tests/test_g7_expand.py (four router tests over every (shape, focus) pair, ::test_INV_PACK_50_only_a_lexeme_focus_can_wear_the_pill now on meaning_select) + ::test_INV_PACK_50_the_written_pack_routes_no_concept_into_a_quoting_prompt + four tests in tests/test_validators_exercise.py",
    "INV-PACK-51": "tests/test_g7_expand.py::test_INV_PACK_51_no_two_tiles_in_one_item_share_a_rendered_label (new — the exercise half, over what G7 actually wrote; the ledger half stays in tests/test_ledger_unit.py and tests/test_g2_band.py)",
    "INV-AUD-08": "tests/test_cast.py (three `inv_aud_08` tests, driven by tests/falsifiers/INV-AUD-08.json; the engine and the pin are in the re-bake key and the accent claim is deliberately not) + tests/test_g8_bake.py (four) + tests/test_loudness.py (two) + tests/test_validators_audio.py (two)"
  },
  "rulings": {
    "B14 — a starved slot": "`StageResult(ok=False)`, message verbatim. Implemented as `StarvedSlot(LookupError)` raised by `_resolve` and caught in `expand`, with EVERY PASS now inside the stage's `try` — pass 1 (resolve + align) was outside it, which is why the one content failure most likely on a real course printed a Python traceback where every other stage prints a line and exits 4. `LookupError` rather than `KeyError` because `KeyError.__str__` renders the repr of its argument and wraps the sentence in quotes. Pinned by test_B14_a_starved_gap_slot_is_a_stage_result_not_a_traceback, which asserts the message text verbatim and that the word `Traceback` does not appear.",
    "B16 — the reader": "`_target_tokens`, `_lemmas_for` and `_ending_target` now read the `analysis` G5 writes onto the candidate record; `ResolvedSlot` carries it so a corpus row and an authored row are the same shape to every consumer. The casefolded-surface fallback for authored text is GONE. An authored row that reaches expansion with `analysis: null` stops the stage naming its candidate_id (`MissingAnalysis`) — the coded rule the frozen contract's docstring promises, which no schema keyword can express because a row rejected before the analyser ran legitimately carries null.",
    "B6 — no locale": "`cast.yaml` and `content/es/audio-manifest.json` carry `language: es` + `accent_claim: unverified`; `tts/cast.py` refuses a cast that still declares `locale:` (the FILE, not just the key — an ignored key is a claim nothing enforces); `ACCENT_CLAIMS` has exactly one member and no `verified` one, because the only thing that can produce that claim is the 300-item native-reviewer sample (B3); F2 blocks a manifest carrying `locale`, missing `accent_claim`, or claiming a stronger one; the pack manifest's existing `audio` block carries `accentClaim` (parsePackManifest reads a fixed list of top-level fields and tolerates the rest, so no schema bump and no request to the validators-ci-loader lane was needed); LOCALE_BY_LANGUAGE survives in config/base.py as a URL template variable only, and its docstring now says so.",
    "B9(b) — lesson 1 is words and fixed phrases": "An item of `LEXEME_FOCUS_MAX_DISPLAY_TOKENS` (2) display tokens or fewer is a lexeme-focus item and reaches S032/S033/S034 and nothing else (`exercises/shapes.py::focus_for_item`). It is a rule about the ITEM because `route()` cannot catch it: a one-token `word_bank_forward` is a well-formed record of a worthless exercise — one tile among four — and so is a cloze that blanks the only word on screen."
  },
  "deviations": {
    "D-S032-NO-ILLUSTRATION": {
      "what": "S032 `picture_select` is declared in config/g7.py::SHAPES with `available_from: P3` and is therefore UNREACHABLE at P2. `route()` refuses it by name (`ShapeNotAvailable`) rather than the stage quietly not emitting it.",
      "why": "`deep/01` §S1: S032's cards are \"illustration + target label + tap-to-hear\" and the prompt quotes the target word. With no illustration the only thing on a card is the label, one of which is the string the prompt already gave away — the item is answerable by reading the prompt, which is the `answerable by copying` defect the reverse-word-bank fix exists to stop, one rung worse because it is the ANSWER rather than a decoy. Nothing can supply that art or reference it: art/README.md's v1 inventory names the S034 cast avatars and NO per-lexeme illustration, docs/art-and-sound.md's outstanding table lists none, and the frozen `exercise` contract has no field for one — `packbuild/sqlite.py` writes `illustration_ref` from `exercise.get(\"illustration_ref\")`, a key the contract does not define, so every row would ship NULL. The brief's instruction was explicit: a recorded deviation, never a null asset reference.",
      "cost": "Two things, both stated rather than absorbed. (1) The NEW WORD pill moves from picture_select to `meaning_select`, which the table already marks `new_word_eligible`; the INV-PACK-50 subset relation (pill-eligible ⊆ lexeme-quoting) is unchanged. (2) `LEXEME_FORM_PLAN` is down to ONE punitive shape, so INV-PACK-07 is no longer discharged by the plan row alone — it is discharged by the row plus `LEXEME_MATCH_SHAPE`, enforced in `_lexeme_pass`: a lexeme is introduced only when a match can also be cut over it, and a lemma that cannot reach both forms is not introduced as a lexeme item at all (it is still taught inside the sentence exercises, which is exactly what already happened to a lemma with no gloss).",
      "reversal": "One table cell (`available_from` back to P2) plus the two requests below. Nothing else in the lane depends on the shape being absent."
    },
    "D-MATCH-UNIT-SCOPED": {
      "what": "A match's five rows are drawn from the UNIT's eligible lemmas in course order, not the lesson's (`MATCH_LEMMA_SCOPE`), and are filed in the lesson of the fifth row. The final cut of a unit is the unit's LAST FIVE rather than a short tail, so a unit ending with two leftovers re-uses three already-matched lemmas and the leftovers still get a second punitive form.",
      "why": "MATCH_PAIRS_PER_EXERCISE is 5 and a real lesson introduces one to three lemmas, so a lesson-scoped match shipped in exactly one place in this repository — the test fixture — and nowhere in a real course. Unit scope is also what `deep/01` §S20 describes (picture-selects, then a 5-pair match, then select-meaning recalls: the match draws on what the learner has met). Without it the INV-PACK-07 construction above is unreachable for almost every lexeme.",
      "cost": "A lemma can appear in two matches of its unit, which gives it a third punitive form. A unit with fewer than five eligible lemmas can build no match and therefore introduces no lexeme items; the count is on the runlog (`lexemes_in_a_unit_too_small_to_match`) rather than silent."
    },
    "D-V5-POS-SETS": {
      "what": "V5's POS clause compares SETS of attested tags (`validators/exercise.py::pos_sets`, gathered from every banded row and every token G1 tagged in context) and blocks only when the distractor's and the answer's sets are non-empty and disjoint.",
      "why": "MEASURED, and it is the defect that stopped the first complete G7 run over the real course: 251 blocking findings, 249 of them this clause, every one of them a distractor the rule core had already drawn from the answer's own POS bucket. `{row[\"lemma\"]: row[\"pos\"]}` keeps one row per lemma (`prima` is a NOUN and a form of `primar`; `frío` is ADJ and NOUN) and a `wrong_form` distractor is an attested SURFACE, so looking it up in a lemma table answers a different question — `viajas` came back PROPN. `DistractorPool` already refuses to decide POS agreement from that map and says so in its docstring; V5 was the last reader that still did.",
      "cost": "The clause is weaker for a genuine cross-POS tile whose surface is attested elsewhere as the answer's POS. That is the correct trade: such a tile is not `eliminable without knowing the word`, which is the whole content of the clause."
    },
    "D-ITEM-KEY-EXCLUSION": {
      "what": "`_decoys` also excludes everything accepted under the key `validators.exercise.item_keys` will file the record under (`_item_key`), and pass 1 indexes each slot's text, translation AND the token a cloze over it will accept.",
      "why": "An authored sentence has no `source_sentence_id`, so `item_keys` files it under its grammar concept and every authored sentence in that concept is ONE item — while the rule core's exclusion set was per-slot. Over the real course V5 reported `distractor 'hermanos' is an accepted answer of another exercise over the same item` twice. The generator and the validator now agree on what an item is by construction rather than by luck."
    }
  },
  "measurements": {
    "the first complete G7 run over the real Spanish course": "`coursekit build es --only g7 --set align_engine=deterministic` over the committed content (1,584 slots, 1,094 corpus-filled, 490 gaps) exited 0 for the FIRST TIME: `es: 1 stage(s) ok`. Before this lane it stopped at `NotEnoughDistractors: needed 3 distractors for 'tardes' (POS , band unbanded)` (B16). 6,100 exercise records; every P2 shape emitted. shapes = {word_bank_forward 528, word_bank_reverse 352, typed_translate_forward 528, typed_translate_reverse 352, fill_in_the_blank 528, complete_the_translation 352, listen_for_the_missing_word 352, tap_what_you_hear 352, type_what_you_hear 352, complete_the_chat 176, speak_this_sentence 352, read_and_respond 352, listen_and_respond 352, type_the_word_ending 102, meaning_select 882, match_pairs 188}. types_emitted = all seven coarse types the phase can reach. shapes_not_available_in_phase = [picture_select, select_the_character].",
    "the three G7 gates over that run": "INV-PACK-07: 0 blocking. INV-PACK-50: 0 blocking. V5: 0 blocking. The V5 number took four measured rounds and each one is a defect this lane fixed: 251 blocking (249 POS + 2 sibling-accepted) -> 84 (the POS set oracle, D-V5-POS-SETS, and the item-key exclusion, D-ITEM-KEY-EXCLUSION) -> 4 (the shape table declaring which option lists are English) -> 0 (the generator reading the gate's POS oracle for its anchor). Every one of the 251 was a false positive against a distractor the rule core had already drawn from the answer's own POS bucket; none of them was a bad distractor.",
    "what the lexeme pass did over the real course": "882 lexemes introduced (`meaning_select`), 188 matches cut, 45 lemmas refused for want of a gloss or an option list, 1 lemma in a unit too small to cut a match from. Every introduced lexeme therefore carries meaning_select + at least one match = 2 punitive forms, which is INV-PACK-07 reporting 0 blocking over 6,100 records rather than over a fixture.",
    "what the alignment bought": "alignment_engine=deterministic (the `align` group is not installed on this Mac; the runlog says the quality is `unmeasured` and that such a pack must not ship), alignment_pairs_written=1,578, word_bank_hints=721, ending_drills_planned=176, ending_drills_written=102. The 102 is new: an authored sentence now carries an analysis, so it gets its ending drills like any other — before B16's read side it could not.",
    "phrase slots over the real course: ZERO, and why": "`phrase_slots=0`. Two reasons, both outside this lane's files and both named in the requests above. (1) `config/g5.py::MIN_TOKENS` is 3, so no short item survives G5 at all; the live run applied ruling B9(b) as a RUNTIME override for lesson 1 only. (2) Even then, `stages/g7_expand.py::_load` keeps the LAST accepted candidate per slot (`candidates[key] = row`), and G6 leaves 6,110 of 9,867 candidates accepted — several per slot — so which survivor ships is whichever G5 wrote last. The lesson-1 diagnostic shard lists its one- and two-token strings first, so the four-token ones won. Deterministic but not a stated rule; recorded rather than changed, because changing it changes which sentence ships in every gap slot of the course and that is a content decision, not a G7 one. The phrase path is therefore covered by three tests (including a real stage run through the CLI) and NOT by the live run.",
    "the audio bake is a three-hour job, which pack-ci cannot hold": "G8 must bake 2,898 distinct clips for this course (6,100 exercise records deduplicated by re-bake key, plus the four cast samples). Measured on this Mac: ~15 clips/minute through synthesise -> master -> encode -> decode -> measure -> re-encode, i.e. about three hours for the bank. `.github/workflows/pack-ci.yml` gives `build-es` 90 minutes. The bake is not slow by accident — INV-AUD-08 measures loudness on the DECODED shipped file and re-encodes until it lands inside half the tolerance — so the fix is not a faster loop; it is a bigger budget, a cached bank keyed on the re-bake key, or a bake job of its own (which is what B2 deleted for a different reason)."
  },
  "contractGapsFiledAsRequests": [
    "`coursekit/artifacts.py::EXERCISE` has no field for an ILLUSTRATION. `packbuild/sqlite.py` writes the pack's `illustration_ref` column from `exercise.get(\"illustration_ref\")` — a key the frozen contract does not define — so the column is NULL for every row of every pack, and S032 cannot ship (D-S032-NO-ILLUSTRATION). REQUEST to the deps/contract lane: add `illustration_ref: string | null` to the EXERCISE record, which moves contract_digest(). REQUEST to the art track: per-lexeme illustrations readable at the card sizes `deep/08` §10 names; art/README.md's v1 inventory does not list them today.",
    "The same record still has no `shape` field and no field for the NEW WORD pill — p2-g7.json's standing requests, unchanged. This lane adds one consequence: with the pill's shape changing from picture_select to meaning_select, the only record-level evidence that the pill clause of INV-PACK-50 holds is still the structural subset relation asserted by `shapes.assert_pill_shapes_quote_lexemes`.",
    "An AUTHORED exercise has no per-item id on the frozen record: `source_sentence_id` is null and nothing carries the candidate_id, so `item_keys` can only file it under its grammar concept. That is right for INV-PACK-07 (the grammar drills need the concept key or each becomes a single-form item) and wrong for V5's cross-exercise clause, which then treats thirty authored sentences as one item. D-ITEM-KEY-EXCLUSION makes the generator agree with that reading; the better fix is a `source_item_id` on the EXERCISE record, which is the deps lane's file.",
    "`config/g5.py::MIN_TOKENS` is 3, so no one- or two-token item can reach G7 from a real build today: the committed lesson-1 candidates (`Hola, hola.`, `Bueno, hola.`) are rejected on the `length` axis and u1/l1's nine slots starve. Ruling B9(b) puts a per-lesson MIN_TOKENS of 1 in G5's lane. G7's half of that ruling is implemented and unit-tested here; over the real tree it was exercised by a RUNTIME override in the live-run driver (not committed), because config/g5.py is not this lane's file.",
    "`config/g5.py`'s over-generation floor is 20 authored candidates per slot, and the lesson-1 window admits about eleven in-vocabulary strings in total (B9). Even with B9(b) applied, u1/l1 fails the floor before any of this lane's code runs. Either the floor is per-lesson too, or lesson 1 is authored against a wider window (B9(a), the lemma-normalisation table)."
  ],
  "liveRun": {},
  "blockers": []
}
