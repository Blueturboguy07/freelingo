{
  "$comment": "Invariant ids the P2 task `p2-g7-expand` turns green in tools/coursekit (G7 expand: the exercise-shape taxonomy, the alignment engines, the distractor rule core, and the exercise validators). REQUIRED OF THE INTEGRATE TASK: merge every docs/owned/*.json into docs/invariants-owned.json, which is the ONLY file `pnpm test:coverage-map` reads. That file is outside this task's lane, so until the merge happens the coverage map does not know about the three ids below. NOTE FOR THE COVERAGE MAP: these ids are owned by PYTHON tests (pytest under tools/coursekit), not by Vitest. `pnpm test:coverage-map` scans TypeScript test names; if it cannot see a pytest name it must be taught to, or these three ids will read as unowned. That is a coverage-map change, not a test change, and it is the first thing the integrate task should check.",
  "task": "p2-g7-expand",
  "module": "tools/coursekit/src/coursekit (exercises/, validators/exercise.py, stages/g7_expand.py, config/g7.py)",
  "phase": "P2",
  "screens": ["S032", "S033", "S034", "S035", "S036", "S037", "S038", "S039", "S040", "S041"],
  "owned": ["INV-PACK-07", "INV-PACK-08", "INV-PACK-50"],
  "owningTests": {
    "INV-PACK-07": "tools/coursekit/tests/test_validators_exercise.py (five tests carrying the id, driven by tests/falsifiers/INV-PACK-07.json) + tools/coursekit/tests/test_g7_expand.py::test_every_form_plan_row_carries_two_punitive_shapes and ::test_INV_PACK_07_the_written_pack_has_two_forms_for_every_missable_item",
    "INV-PACK-08": "tools/coursekit/tests/test_validators_exercise.py (four tests carrying the id, driven by tests/falsifiers/INV-PACK-08.json) + test_g7_expand.py::test_INV_PACK_08_the_written_packs_answers_are_all_in_the_declared_register. PARTIAL — see `partial` below.",
    "INV-PACK-50": "tools/coursekit/tests/test_validators_exercise.py (four tests, driven by tests/falsifiers/INV-PACK-50.json) + test_g7_expand.py (four router tests carrying the id, over every (shape, focus) pair rather than a sample)"
  },
  "partial": {
    "INV-PACK-08": "HALF IMPLEMENTED, DELIBERATELY. The registry line has two clauses: (1) every accepted answer is in the unit's declared register, and (2) for Japanese every script variant (kanji+okurigana, all-kana, katakana) is enumerated as a set. Clause (1) is implemented here for SPANISH ONLY, as V6 with a marker table in config/g7.py::ES_REGISTER_MARKERS. Clause (2) is P7's: it needs the characters stage, JmdictFurigana and SudachiPy reading_form(), and EC-GRD-16 widens it to FOUR classes, not three — the alternate-okurigana headwords of the same JMdict entry (`引越し` for `引っ越し`, `落す` for `落とす`) are a fourth class the registry line does not name. Rather than half-implement it, V6 returns a BLOCKING finding for any language outside V6_IMPLEMENTED_LANGUAGES, so a Japanese pack cannot reach a pass through this validator at all. tests/falsifiers/INV-PACK-08.json carries a `japanese_half` block that exists to be refused, and test_INV_PACK_08_japanese_is_refused_not_passed asserts the refusal. The id should NOT be treated as fully green until P7 lands clause (2)."
  },
  "notOwnedHere": {
    "INV-PACK-13": "the ingest licence allow-list. G0's lane; a P2 entry criterion.",
    "INV-PACK-15": "codec/bitrate/bytes against the audio budget. G8's lane (F2).",
    "INV-PACK-16": "the ja characters stage coverage. P7 (F5).",
    "INV-PACK-17": "attribution reachable from S152. G9's lane (F3).",
    "INV-MIS-06": "the RUNTIME twin of EC-MIS-04 — the fallback ladder when a mistake's other formats are filtered out by preference or suspension. That is packages/core/session, P3/P4. INV-PACK-07 is the BUILD gate it depends on and cannot see: EC-MIS-11 says so in as many words."
  },
  "rulings": {
    "S043 Put the events in order": "Out of v1 by founder ruling (no grading contract, no taxonomy home). Made unreachable, not merely unwritten: it has no row in config/g7.py::SHAPES, no member in config/base.py::EXERCISE_TYPES, and config/g7.py::FORBIDDEN_SHAPE_IDS names it so shapes.shape() and shapes.route() both raise ForbiddenShape carrying the ruling text rather than a KeyError that reads like a typo. test_g7_expand.py::test_S043_the_instruction_string_is_never_a_renderable_literal walks every module with ast and fails if the instruction ever appears as a non-docstring string literal — with its own falsifier, because a grep over the whole file would fail on the three modules that quote S043 while stating the ruling.",
    "The distractor re-ranker": "scope2/00 §2.3 puts an LLM on G7 as a RE-RANKER ONLY. No hosted model is reachable from this environment and no API key exists, so exercises/distractors.py::rerank() is declared with the contract a future implementation must satisfy (a permutation of the rule core's output, never a new candidate, so V5 stays true whatever the model says) and raises RerankerUnavailable. It has NO CALL SITE, and test_the_reranker_has_no_call_site walks the module with ast to prove it — because 'the hook is unimplemented' behind a false flag is still a call site somebody flips on. A heuristic wearing the re-ranker's name would put a claim in the pack manifest that is not true.",
    "The alignment fallback is a declared engine": "pyproject.toml's rule is that a stage which needs an absent dependency group fails loudly and never degrades to a worse tool. So DEFAULT_ALIGNMENT_ENGINE is `simalign` and an absent `align` group is exit 3 with `uv sync --group align`. CI does not carry torch, so CI reaches the deterministic aligner by NAMING it: `coursekit build es --only g7 --set align_engine=deterministic`. Whichever ran is written to the runlog entry as alignment_engine / alignment_quality / alignment_note, and the fallback's note says in as many words that a pack built with it must not ship. There is no `auto`.",
    "meaning_select is dropped rather than half-authored": "A newly introduced lexeme gets BOTH recognition forms or neither. If the alignment produced no gloss for the lemma, or there are not enough same-POS same-band glossed neighbours to fill the option list, stages/g7_expand.py::_lexeme_drafts returns nothing at all — because a lexeme item with one punitive form fails INV-PACK-07, and the word is still taught inside that lesson's sentence exercises.",
    "A match is five items' form, not one item's": "S033 tags five lexemes. validators/exercise.py::item_keys files a record with no source_sentence_id under ONE KEY PER TAGGED LEMMA, so a five-pair match counts as a second form for each of its five lexemes. EC-MIS-04 names exactly this case ('the left-hand lexeme is the queued mistake'), and a composite key would have failed INV-PACK-07 for every match in every pack, for a reason that is not true."
  },
  "measurements": {
    "eng-spa alignment, hand-checked gold sample": "NO eng-spa F1 IS PUBLISHED for SimAlign (deep/10 §S7 reports eng-fra .94, eng-ces .87, eng-deu .81, eng-ron .65, eng-fas .67, eng-hin .55 and nothing for spa or jpn). A 15-pair eng-spa gold sample was hand-aligned for this lane and lives in tools/coursekit/tests/test_alignment.py::GOLD, including the cases a positional heuristic gets wrong: pro-drop (`Vivo` <- `I live`), periphrastic negation (`I do not eat` -> `No como`) and a two-token courtesy (`please` -> `por favor`). Measured 2026-09-12, DeterministicAligner against that sample: precision .783, recall .711, F1 .745, micro-averaged over 76 hand-aligned pairs across 15 sentences. SimAlign itself was NOT measured — torch is not installed here — so that row of the table is a skipped test, not a number. The .745 is a REGRESSION FLOOR (FALLBACK_F1_FLOOR = 0.70), never a quality claim: the sample is 15 sentences of A1 fixture text, all with a length ratio under 2:1, which is the easiest case a positional aligner ever sees.",
    "the fallback's real behaviour": "Measured, and it is not restraint: inside the length-ratio gate the deterministic aligner emits EXACTLY min(len(source), len(target)) pairs whether or not it has any lexical evidence, because a proportional diagonal always scores 1.0 on position and the greedy always finds it. FALLBACK_MIN_SCORE alone never reduces the pair count — that was measured and the module docstring now says so. FALLBACK_MAX_LENGTH_RATIO = 2.0 is the only refusal it has, and it was added because the module's 'allowed to produce nothing' claim was otherwise false."
  },
  "contractGapsFiledAsRequests": [
    "coursekit/artifacts.py::EXERCISE has no `shape` field and `additionalProperties: false`, so the sixteen product-map shapes are projected onto eight coarse EXERCISE_TYPES and the shape is recovered from (type, instruction line of prompt) by shapes.shape_of_record. Every draft round-trips under a 10,000-case property test, so nothing is lost today — but the player at P3 has to decode a prompt to know what to render. REQUEST: add `shape` (enum over config/g7.py::SHAPES ids) to the EXERCISE record. That file is the deps lane's and changing it moves contract_digest(), so it is a deliberate cross-lane change, not something to slip in.",
    "The same record has no field for the NEW WORD pill, and no field for which alignment engine produced its `alignment` pairs. The pill clause of INV-PACK-50 is discharged instead by a structural fact (the pill-eligible shapes are a subset of the lexeme-quoting shapes, re-asserted on every routing decision by shapes.assert_pill_shapes_quote_lexemes), and the engine is written to the runlog. Both are sound; both would be simpler as fields.",
    "config/base.py::VALIDATOR_IDS has no id for INV-PACK-07 or INV-PACK-50. They run as STAGE GATES inside G7 (the stage refuses to write an artefact that fails them) and as functions the tests call with the committed falsifiers. REQUEST: add `F6` (INV-PACK-07) and `F7` (INV-PACK-50) to VALIDATOR_IDS and VALIDATOR_TITLES so `coursekit validate` reports them on a pack it did not build itself.",
    "config/g7.py::DEFAULT_REGISTER_BY_SLOT and REGISTERS resolve unit_assignment.register_slot to a concrete register. That mapping is arguably G3's (config/curriculum.py, which is empty and owned by p2-g3-curriculum). It lives here because V6 is the only consumer today; if G3 lands its own, the two must be reconciled into config/base.py rather than duplicated.",
    "Registry.reset_for_tests() in coursekit/__init__.py clears the entries AND the discovered flag, but rediscovery calls importlib.import_module on modules already in sys.modules, so @register_stage never runs again. Any test using tests/conftest.py::empty_registry therefore unregisters every REAL stage for the rest of the pytest session, and the CLI then exits 2 for a stage that is plainly registered. This lane works around it with an autouse fixture in test_g7_expand.py that reloads the stage module; the fix belongs in Registry.reset_for_tests (re-run the decorators, or drop the modules from sys.modules) or in conftest, neither of which is this lane's file."
  ],
  "falsifiers": "tools/coursekit/tests/falsifiers/INV-PACK-07.json, INV-PACK-08.json, INV-PACK-50.json. Each carries a `falsifier` block that MUST fail, a `repair` block that MUST pass, and a third block for the subtler failure mode of the same invariant (a non-punitive second form; a mixed-register answer; a lexeme-quoting prompt quoting something outside the tag set). Every record in every block is validated against the frozen `exercise` schema on load, so a falsifier that drifts from the contract fails loudly instead of being rejected by the gate for the wrong reason."
}
