# P1 Engine

Source: the approved plan `~/.claude/plans/vectorized-coalescing-sun.md` (phase table row P1) and the P1–P8 workflow script `~/duolingo-research/build/p1-p8.workflow.js`. Needs a device for verification: **false**.

## Brief

Build every module of packages/core listed in the plan's Engine table except hub/notifications/course/stories/radio/widget: session/ (queue generation, the state machine idle→armed→grading→banner→…, the nine-field resume row, one-in-flight rule, quit rules, the TEN session flavours S057–S066 as a config table), grading/ (three tiers, six soft notes, three NAMED typo guards as config, authored alternates, multi-gap, the accuracy denominator), scheduler/ (FSRS via ts-fsrs with the 0.6× early-review guard, clamped elapsed, idempotent attempts keyed (session_id, exercise_index), Today's Review/Daily Refresh pools), economy/ (ONE config table with every constant named and dated: goal tiers 10/20/30/50, XP per flavour, boosts + boostGraceSeconds, quest scaling, freeze price, cosmetics, maxRolloverDeferralSeconds=300, streakRepairsPerMonth=1; the session-flavour matrix; per-mode daily ladders; two XP counters), ceremony/ (ordered predicate queue with scope ∈ {account, course}, the single exclusive commit, the bundle reward selection; extend CER-01), path/ (node state as a pure function, linear unlock incl. the unit boundary rule, jump-here/section tests, Score floor, Daily Refresh), packs/ (six-state enum incl. unverified, partial vs corrupt, ed25519 signature verify using packages/schema/keys/pack-signing.pub via a pure-TS verifier, quarantine on major bump, audio resolution at generation), migrations/ (schema version registry + golden-DB corpus: one fixture per user_version, a CI job migrating every fixture forward with row-level equality), security/ (SEC-01 sanitiser at storage time, SEC-02 no interpolated SQL grep gate), day/ (extend P0: unlived days, grace window, rollover with bounded deferral, freeze walk, recovery challenge, monthly Streak Repair, Perfect Streak). Also the ART-AND-SOUND track starts here in parallel (lane art/ and packages/ui/src/assets): the parrot mascot as SVG with five poses (idle, happy, sad, cheer, sleepy) readable at 64 px and 240 px, a 4-character cast with speech-bubble avatars, node/chest/trophy/story-cover art, and a generated sound bank (correct/wrong stings, fanfare, combo shimmer, chest, streak, earcon) produced by a Python synthesis script and encoded to Opus + AAC. Invariant sections to own: §1–§10, §13, §14 (engine parts), SEC-01/02; add every id to docs/invariants-owned.json and make coverage-map green. Gate: pnpm test green with every owned id tested; falsifier inputs committed per invariant; Stryker score recorded; the headless 30-day two-course four-zone journey runs as a test.
