# P3 Lesson player

Source: the approved plan `~/.claude/plans/vectorized-coalescing-sun.md` (phase table row P3) and the P1–P8 workflow script `~/duolingo-research/build/p1-p8.workflow.js`. Needs a device for verification: **true**.

## Brief

The app: onboarding S001–S009 (import reachable from onboarding), path canvas + nodes S010–S028 with the measured geometry, session shell + every exercise type S029–S042 (S043 out of v1), all ten flavours wired (S057–S066), banners/report sheet/interstitials/quit/resume/concurrent dialogs S044–S053 (S053 config-flagged off), modality gates S054–S055, the ∞ meter, design system in packages/ui from the measured tokens (the 3D button with the lip on a separate layer, cards, chips, progress bar green→gold at combo 6, tab bar, unit header per-unit colour, speech bubbles), Nunito bundled, the mascot poses behind MascotRenderer with Skia/Reanimated crossfades, the sound bank wired with class-based cue mixing (INV-SND-01), haptics best-effort. Read scope/09-deep-capture-web-guest.md and the 236 reference frames in ~/duolingo-research/screens (view the PNG/JPG files with the Read tool) for pixel parity. Tests: unit/property for §11 MOD/A11Y/TYP/I18N and §12 SND ids; Playwright token-conformance against scope/10 on the Expo web target at 614 CSS px (pnpm e2e:tokens) plus the curated ~15 masked parity frames; Maestro flows on the iOS simulator and Android emulator: onboarding → 3 lessons → kill/resume → quit/end → placement → jump-here; the a11y-tree walk answering one instance of every exercise type (INV-A11Y-04); the typography matrix {3 scales}×{3 widths}×{light,dark} as native snapshot baselines; ar/he launch-arg locale test. Gate: all of the above green in CI with artefacts.
