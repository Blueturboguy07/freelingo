# P8 Release

Source: the approved plan `~/.claude/plans/vectorized-coalescing-sun.md` (phase table row P8) and the P1–P8 workflow script `~/duolingo-research/build/p1-p8.workflow.js`. Needs a device for verification: **true**.

## Brief

Release engineering: EAS config for Starter tier with eas.json profiles (preview simulator build, production), a signed release APK and an iOS simulator .app produced by CI and attached as artefacts (TestFlight submission itself needs App Store Connect credentials that do not exist here — write docs/RELEASE.md with the exact commands and leave the submit step manual), README honesty block finalised (parity definition per the plan, provenance % per pack, provisional defect rates, no-reference surfaces list, CEFR policy, licence split, exceeds-Super note for Roleplay), docs/ (invariants map, pack format, contributing, ci), the full manual D-kind checklist file, Stryker threshold enforced, a final full CI run green, and a publik listing draft (docs/PUBLIK-LISTING.md) for ~/duolingo-research's owner to post. Gate: all workflows green on main; artefacts present; docs complete.
