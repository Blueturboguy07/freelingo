# P5 Native surfaces

Source: the approved plan `~/.claude/plans/vectorized-coalescing-sun.md` (phase table row P5) and the P1–P8 workflow script `~/duolingo-research/build/p1-p8.workflow.js`. Needs a device for verification: **true**.

## Brief

Widget: iOS WidgetKit target via @bacons/apple-targets (small, medium, lock-screen accessory) and Android Glance 1.2 module, reading ONLY the shared snapshot (INV-WID-01..07), total state machine incl. STREAK_SAFE_GOAL_UNMET, draw-time remaining, debounced reloads, deep link with course id; snapshot writer in the app. Speaking exercises with expo-speech-recognition (on-device where available, continuous:true, feature-detect locales, the five-cause gate incl. no-recogniser-for-locale, transcript match against pack-stored tokens, skippable, never a heart) and the permission ladder S055 (iOS two-grant AND). TTS fallback via expo-speech behind the audio resolver (INV-AUD-01/02). Explain My Answer + Roleplay behind the Explainer interface: evaluate expo-local-llm (Apple FM iOS 26+, Gemini Nano API 26+) and @react-native-ai/apple; BYOK via fetch with the four Anthropic headers, mocked in tests; the three-condition render gate + tap-time reachability (INV-LLM-01, EC-PLAT-17); the ja masu/desu suffix gate table (EC-PLAT-18). Share cards via an off-screen Skia canvas. Tests: §17, §20, SEC-03 ids; Maestro widget deep-link flow; ASR flows on the emulator; D-kind manual items recorded as a checklist file with what could not be automated. Gate: green in CI with artefacts.
