# P2 Spanish pack

Source: the approved plan `~/.claude/plans/vectorized-coalescing-sun.md` (phase table row P2) and the P1–P8 workflow script `~/duolingo-research/build/p1-p8.workflow.js`. Needs a device for verification: **false**.

## Brief

Build tools/coursekit for real: G0 ingest (Tatoeba per-language exports + a capped OPUS/NLLB stream ≤2,000,000 pairs via the OPUS API, per-sentence licence provenance, the licence allow-list ACTIVE BEFORE the first byte is read — INV-PACK-13 — NC/ND excluded at ingest, uppercase sentences_CC0 filename, JParaCrawl forbidden), G1 analyze (spaCy 3.8 es_core_news_md via uv; ledger_unit = lemma), G2 band (frequency from hermitdave CC BY-SA-4.0 declared share-alike; ELELex CC BY-NC-SA used ONLY because packs are NC), G3 curriculum solver (constraint solver, not an LLM: assign lemmas + grammar concepts to units under the template with new-item rate and recycling constraints; the section CEFR label is a G3 output), G3 template from content/es/curriculum.yaml which you AUTHOR ORIGINALLY (~10 Section-1 units with can-do titles in the style of "Order at a café", ~25–30 grammar concepts, target lexemes ≈1,000 total for A1; never copy Cervantes or Duolingo text), G4 ledger-constrained selection from the corpus with a gap list, G5 gap-fill where YOU author candidate sentences into candidates.jsonl (closed vocabulary; generate-and-reject through G6), G6 validate (KenLM perplexity band on a corpus sample; LanguageTool es via a local Java server; back-translation consistency using your own judgement recorded as a rubric score), G7 expand (SimAlign for word banks/hints; exercise shapes per deep/01: picture-select, match pairs, meaning select, word-bank translate both directions, typed translate, complete the chat, listening family, gap-fill family, read/listen-and-respond, speak; distractors from the rule core same-POS + same-band + morphological wrong-form; every missable item has ≥2 authored forms), G8 bake (Kokoro es voices ×4 cast roles, Opus 20 kbps via opusenc, content-addressed), G9 package (read-only SQLite pack per packages/schema, exercise_item_tag join, per-sentence attribution owner strings, signed manifest with provenance %, validator report, audio budget ≤120 MB incl. a placeholder for stories/radio, ledger_unit declared). Validators V1–V12 + INV-PACK-06..16 as `coursekit validate` in pack-ci.yml. `coursekit sample` emits the 300-item stratified sheet; since no paid native reviewer exists in this run, a separate Opus reviewer agent scores it and the manifest records the rate as PROVISIONAL (unreviewed by a paid native speaker) — the README says so. ROUND 3 — FOUNDER RULINGS ON THE P2 BLOCKERS (read ~/freelingo/docs/P2-BLOCKERS.md, section 'Founder rulings — 2026-09-12', and docs/P2-REPORT.md first; P2 stopped red twice on these and they are now DECIDED): B9 lesson 1: add a G1 adapter lemma-normalisation table (buen/buena/buenos/buenas → bueno and any prenominal form the curriculum names), per-lesson MIN_TOKENS=1 for verb-less windows so lesson 1 ships words and fixed phrases (picture-select + match pairs, as Duolingo's level-1 lessons do), and a G3 validator that fails when a declared target lemma is unreachable by the pinned lemmatiser; re-key only rows whose ledger_digest changes. B16: carry G1 analysis across G5→G7 by extending the frozen CANDIDATE contract (bump the digest; the deps task owns it). B14: StageResult(ok=False). B17: V11 hard-fails only across sections, warns within. B1b: course-global lesson numbers. B3: no paid reviewer exists — score the 300-item sample with a dedicated Opus reviewer task (REVIEWER_KIND_AGENT), write the rate into the manifest with the PROVISIONAL string verbatim, and let gate_passed() accept an agent-scored rate ≤2% for this run; docs/RELEASE.md lists the paid native review as a release prerequisite. B6: Kokoro is the voice engine; manifests carry language: es + accent_claim: unverified instead of locale es-ES; the rubric checks accent consistency; blended cast roles stay. B5: S152 now has a product-map row (Surface 16). B8 (LanguageTool sidecar in build-es) and B13 (align group) are resolved — keep them. Also run pnpm format once in the deps task. The phase is DONE only when coursekit build es → validate es exits 0 in pack-ci with V8 naming a real engine, the pack artefact exists, and packages/core loads it. Deliver content/es pack v0 built in CI and attached as a workflow artefact. Gate: coursekit validate green, V1–V4 100%, audio within budget, every attribution-required sentence has an owner string, pack-ci green, the pack loads in packages/core's pack loader tests.

## Round-4 tasks that were in flight when the Claude run was stopped (branches pushed to origin)

### `p2r4/author-u01-u06`

- lane: `content/es/candidates/u01-l01.jsonl`, `content/es/candidates/u01-u06.jsonl`, `content/es/candidates/u01-u06-r4.jsonl`, `content/es/curriculum.yaml`, `tools/coursekit/src/coursekit/config/g7.py`, `tools/coursekit/src/coursekit/validators/v6_register.py`, `tools/coursekit/tests/validators/test_v6*.py`, `docs/owned/p2r4-author-u01-u06.json`
- invariant ids: INV-PACK-06, INV-PACK-08, INV-PACK-10
- depends_on: —

Author the units 1–6 half of B19 and settle the one contradiction inside it. Verified live this round: pack-ci run 34708331193 on 42b0be3, job build-es, `g5 failed: 22 slot(s) authored below the over-generation floor of 20: u1/l3/s1 (0), u1/l3/s3 (0), u1/l3/s4 (0), u1/l3/s5 (0), u1/l3/s6 (0), u1/l3/s7 (0), u4/l24/s6 (0), …` — exit 4, and validate-es skipped on `needs:`. (1) SEVEN EMPTY SLOTS, 20 candidates each = 140 rows: u1/l3/s1 (reserves `yo`), u1/l3/s3 (reserves `usted` — see (3)), u1/l3/s4 s5 s6 s7 (14 known lemmas, subject_pronouns), u4/l24/s6 (reserves `suelo`, 127 known lemmas, definite_articles). Author against the REGENERATED brief `content/es/authoring/gap-brief.jsonl` — 494 slots, ledger digest `e041abe57f6f0836`, re-confirmed byte-identical against CI artefact es-gap-brief-42b0be3 (0 rows differing). A candidate keyed to the old 490-slot brief is the exact defect B19 is. (2) FOUR DUPLICATE-STARVED SLOTS, u1/l2/s2 s4 s6 s7: each already has 20 rows and zero survivors (3–7 out_of_vocabulary, 15–17 duplicate) because the unit's window admits few enough strings that earlier slots consume them. These need genuinely NEW texts, not re-keys; `authored_candidates_paths()` evaluates files in sorted filename order, so check every new string against what u01-l01.jsonl and the earlier u01-u06 rows already spend. INV-PACK-10 forbids patching a candidate into a window it was not written for — discard and write fresh; orphans belong in content/es/candidates-orphaned/ with a reason. (3) THE `usted` CONTRADICTION, and it is the one founder line in this phase: curriculum.yaml:420 reserves `usted` as a target lexeme for u1/l3/s3 in a course whose register is `tu` (config/g7.py::DEFAULT_REGISTER_BY_SLOT['binary_t_v']), where V6 treats an `usted` marker in an accepted answer as BLOCKING — the rule 218 rows were rewritten for at B10. No candidate that teaches the slot's reserved lemma can pass V6 and no candidate that passes V6 can teach it. DEFAULT, unless ~/duolingo-research/DECISIONS-LOG.md carries an explicit founder line saying otherwise: scope V6's binary_t_v rule to exempt the single slot that INTRODUCES the pronoun, record the exemption in content/es/review/RUBRIC.md and the manifest so a reviewer is told, and carry a test named for INV-PACK-08 proving the exemption is slot-scoped (u1/l3/s3 only) and that every other slot still blocks. Do NOT take option two (drop `usted` from target lexemes and teach the form inside formal_informal_address u23) without that founder line: it moves every ledger_digest in unit 1 again, which is B19's own failure mode re-run. (4) B3 TEXT DEFECTS in this range, from content/es/review/scores-method.md: row 6 u2/l9/s6 `¿Son cuarenta, señor?` (`son` is not existential; the English asks `¿Hay cuarenta?`), row 49 u6/l36/s7 `En agosto yo tengo veinte años.` (turning an age is `cumplir`), row 50 u6/l36/s7 `Es una costa de un sur europeo.` (a cardinal-direction noun takes no indefinite article). Fix the texts in place in the candidate files; the redraw and re-scoring belong to the sample lane, not here. Verify locally with `uv run coursekit build es --stop-after g5` in the BACKGROUND with a log file (it is ~24 min in CI) and poll with short `sleep 90; tail -n 20 <log>` calls; report the G5 census (read/written/rejected, stale_ledger, out_of_vocabulary, duplicate, length, orphan_authored_slots) verbatim, never a summary of it.

### `p2r4/author-u07-u23`

- lane: `content/es/candidates/u07-u14.jsonl`, `content/es/candidates/u15-u23.jsonl`, `content/es/candidates/u07-u23-r4.jsonl`, `docs/owned/p2r4-author-u07-u23.json`
- invariant ids: INV-PACK-06, INV-PACK-10
- depends_on: —

Author the units 7–23 half of B19, and fix the B3 defect patterns that live in the same files. (1) SIX EMPTY SLOTS, 20 candidates each = 120 rows, all with wide windows (521–712 known lemmas), so this is authoring and not a window problem: u16/l96/s6 (reserves `mediodía`, time_expressions), u17/l102/s6 (time_expressions), u21/l121/s8 (reserves `peinar`, reflexive_daily_routine), u23/l131/s6 (reserves `molestia`, formal_informal_address), u23/l132/s7 and u23/l134/s6 (formal_informal_address). Author against the regenerated brief content/es/authoring/gap-brief.jsonl — 494 slots, ledger digest `e041abe57f6f0836`, confirmed identical to CI artefact es-gap-brief-42b0be3. (2) ONE STARVED SLOT, u17/l101/s8: 20 authored rows, all 20 rejected `out_of_vocabulary` under the post-B9 ledger. These are not re-keyable — the texts are wholly outside the new window and need rewriting. INV-PACK-10 forbids patching a candidate into a window it was not written for; write fresh rows and move the dead ones to content/es/candidates-orphaned/ with a reason. (3) u23 IS THE FORMAL/INFORMAL UNIT AND THE COURSE REGISTER IS `tu`: V6 treats an `usted` marker in an accepted answer as blocking (config/g7.py::DEFAULT_REGISTER_BY_SLOT['binary_t_v'], the rule 218 rows were rewritten for at B10). Do not reach for `usted` to teach formal address in u23 unless the u01-u06 lane's V6 exemption has landed and names the slot; coordinate through docs/P2-BLOCKERS.md B19 rather than editing V6 here — that file is the other lane's. (4) B3 TEXT DEFECTS in this range, from content/es/review/scores-method.md, including all three repeated patterns that carry the measured rate from 4.00% to 2.00%: rows 98+100 u15/l87/s6,s7 `Continúa hasta la rotonda/el puente.` glossed "You carry on…" (imperative/3sg glossed as 2sg declarative; `continúas` is not accepted) — one template, two rows; rows 123+124 u17/l98/s5 `hay nubes` glossed "There is clouds" (the English prompt is not English) — one template, two rows; rows 150+151 u18/l108/s7 `Los dormitorios/salones son limpios.` (a room's cleanliness is `estar limpio`) — one ser/estar error, two rows. Plus the one-offs: row 79 u11/l64/s6 `No encuentro mi cartera.` glossed "I lost my wallet" (`perdí`), row 121 u16/l95/s3 `Hoy tendremos pescado de cena.` glossed present (`tenemos` is not accepted), row 171 u19/l112/s4 `Mi hermana avisa la propuesta.` (`avisar` does not take the announced thing as a direct object). Fix the texts here; the redraw and the published rate belong to the sample lane. (5) Also worth fixing while in these files, named in scores-method.md and not counted as wrong: `cancha` appears in three rows of one 300-item sample and is Latin-American where a peninsular course says `pista` or `campo`. Run `uv run coursekit build es --stop-after g5` in the BACKGROUND with a log and poll with short `sleep 90; tail -n 20 <log>` calls; paste the G5 census verbatim.

### `p2r4/author-u24-u30`

- lane: `content/es/candidates/u24-u30.jsonl`, `content/es/candidates/u24-u30-r4.jsonl`, `docs/owned/p2r4-author-u24-u30.json`
- invariant ids: INV-PACK-06, INV-PACK-10
- depends_on: —

Author the units 24–30 half of B19 — the largest share, 11 of the 29 unfilled slots. (1) NINE EMPTY SLOTS, 20 candidates each = 180 rows: u25/l145/s4 (reserves `ojo`, estar_gerund, 787 known lemmas); u27/l154/s4 s5 s6, u27/l156/s3, u27/l157/s5 s6, u27/l158/s7 (comparatives_basic, reserving `doble` … `total`, 834–855 known lemmas); u29/l168/s6 (reserves `cortado`, yes_no_questions, 906 known lemmas). Every window here is wide, so a starved slot in this range would be an authoring failure and not a ledger one. Author against the regenerated brief content/es/authoring/gap-brief.jsonl — 494 slots, ledger digest `e041abe57f6f0836`, confirmed byte-identical to CI artefact es-gap-brief-42b0be3 (0 rows differing across 494 slot rows). Candidates keyed to the frozen 490-slot brief (digest `e1dcba85…`) are exactly the 18-orphan defect B19 names; check the digest before writing a line. (2) TWO STARVED SLOTS, u25/l143/s8 and u25/l144/s8: 20 authored rows each, all 20 rejected `out_of_vocabulary` under the post-B9 ledger. Not re-keyable — write fresh texts inside the new window and move the dead rows to content/es/candidates-orphaned/ with a reason, because INV-PACK-10 forbids patching a candidate into a window it was not written for and the re-key script tools/coursekit/scripts/rekey_authored_candidates.py has already done every row whose digest merely moved (stale_ledger 9,269 → 0). (3) The floor is 20 per slot and G5 rejects hard on out_of_vocabulary / duplicate / length; 20 candidates that all fail is a failed stage, not a filled slot, which is what produced the u1/l2 and u25/l143 starvation in the first place. Over-generate honestly rather than padding with near-duplicates: `duplicate` was 2,965 of 3,646 rejections at the last census. (4) No B3 review defects fall in this range; do not edit content/es/review/*, that is the sample lane's. Verify with `uv run coursekit build es --stop-after g5` in the BACKGROUND with a log file, polled by short `sleep 90; tail -n 20 <log>` calls (the CI build is 23m56s), and paste the G5 census verbatim — read/written/rejected/reject_rate, stale_ledger, out_of_vocabulary, duplicate, length, new_lemma_budget, orphan_authored_slots, and the `N gap slots · M filled` line.

### `p2r4/pipeline-g6-to-g9`

- lane: `tools/coursekit/src/coursekit/stages/g5_gapfill.py`, `tools/coursekit/src/coursekit/stages/g6_validate_language.py`, `tools/coursekit/src/coursekit/stages/g7_expand.py`, `tools/coursekit/src/coursekit/stages/g8_bake.py`, `tools/coursekit/src/coursekit/stages/g9_package.py`, `tools/coursekit/src/coursekit/packbuild/`, `tools/coursekit/src/coursekit/validators/`, `tools/coursekit/src/coursekit/signing.py`, `tools/coursekit/src/coursekit/sign.py`, `tools/coursekit/tests/`, `content/es/audio-manifest.json`, `packages/core/src/packs/`, `.github/workflows/pack-ci.yml`, `docs/pack-provenance.md`, `docs/owned/p2r4-pipeline-g6-to-g9.json`
- invariant ids: INV-PACK-06, INV-PACK-07, INV-PACK-10, INV-PACK-13, INV-PACK-14, INV-PACK-15, INV-PACK-17, INV-PACK-40, INV-PACK-41, INV-PACK-50, INV-PACK-51, INV-AUD-08
- depends_on: author-u01-u06, author-u07-u23, author-u24-u30

Take the completed candidate bank through G6–G9 and produce the first real Spanish pack this project has made on `main`, then prove the gate items that have been NOT PROVEN for three rounds because no pack ever existed. Starts only after the three authoring lanes land. (1) Run `uv run coursekit build es` end to end — ALWAYS in the BACKGROUND with a log file, polled by short `sleep 90; tail -n 20 <log>` calls; the CI build-es job is 23m56s and a foreground run kills the agent. (2) Fix what breaks in G6–G9. The one prior end-to-end run (the expand lane's tree, now gone) got G7 over the whole course, G8 over units 1–3 and G9 to a pack, with `coursekit validate es` at 11/17 green and 210 blocking findings — treat that as the known list, not as passing evidence; on `main` the same validators sit at 5/17 green, 33 blocking over a G0–G5 tree. (3) Prove, each with the id in the test name and the real command output pasted: INV-PACK-06 V1–V4 at 100% with zero violations; INV-PACK-07 every missable item carrying ≥2 authored forms (0 blocking over 6,100 records on the dead tree is not evidence for this one); INV-PACK-14 V8 recording `languagetool/6.6/es`, `agent_rubric/v1`, `perplexity none`, `degraded_to grammar_only` from a G6 run ON THIS TREE rather than from the CI sidecar step; INV-PACK-15 the audio manifest asserting codec=opus / 20 kbps / total bytes against the 120,000,000 B budget on REAL BYTES across lessons + stories + radio — today content/es/audio-manifest.json declares 98,000,000 B and only 580,184 B over 125 clips was ever measured; INV-PACK-17 + V10 zero UNRESOLVED licences and an attribution owner on every required row, reachable from the credits surface; INV-PACK-40 `ledger_unit` declared exactly once in the manifest (the grep half over tools/coursekit is already green, the manifest half has never had a manifest); INV-PACK-41 a presentation-only rebuild leaving every item id intact — that needs two builds, so plan for it; INV-PACK-50 no grammar-concept or script_unit item routed into a lexeme-quoting prompt; INV-PACK-51 no two ledger items sharing (normalized_form, reading_form, POS); INV-AUD-08 packed clips within the declared loudness tolerance with the synthesis ENGINE in the re-bake key (Kokoro by founder ruling B6, Azure is dead). (4) Then `uv run coursekit validate es --pack <pack> --report <report>` to exit 0, and `uv run coursekit sign es`, which has never run because it needs a G9 manifest. (5) Run the loader test with FREELINGO_REAL_PACK pointed at the built pack. Note the property that already holds and must keep holding: unset, the file SKIPS (1 file / 6 tests); set to a nonexistent path, all 6 FAIL with ENOENT rather than skipping — so the step cannot be green on a missing pack. It has simply never had one. (6) One small code fix while you are in the file: G5's orphan message blames the B1b per-unit-vs-global lesson keying for a check that has two causes, and it misdiagnosed B19 for a whole round. Make it say the slot is not in this build's gap list, and offer the keying explanation as one possibility. (7) pack-ci's `validate-es` job has skipped on `needs: build-es` on all three passes — that is a real dependency and not the green-because-skipped failure mode (pipeline-ready was green each time), so leave the `needs:` alone; the fix is build-es going green. Only CI artefacts count as evidence (plan §build workflow step 3).

### `p2r4/sample-accent-rate`

- lane: `tools/coursekit/src/coursekit/sample.py`, `tools/coursekit/src/coursekit/config/sample.py`, `tools/coursekit/tests/test_sample*.py`, `content/es/review/RUBRIC.md`, `content/es/review/README.md`, `content/es/review/scores-method.md`, `content/es/review/scores.jsonl`, `content/es/cast.yaml`, `docs/owned/p2r4-sample-accent-rate.json`
- invariant ids: INV-PACK-14, INV-AUD-08
- depends_on: pipeline-g6-to-g9

Close B18 and publish a real wrong-item rate instead of `None`. Two halves; the code half can start immediately, the redraw half waits on the pack. (1) B18, CODE, START NOW: `SampleItem` (tools/coursekit/src/coursekit/sample.py:90) carries exercise_id, unit/lesson index, type, provenance, prompt, accepted_answers, distractors, source_text, source_translation — and NO clip reference and NO voice role, so the `accent_consistency` dimension that founder ruling B6 made the only accent check there is has nothing to listen to. Add a clip ref and a voice role sourced from the G8 bake, and make the dimension scoreable. The trap to handle deliberately: `REVIEW_DIMENSIONS` in config/sample.py is the five text dimensions, and adding a sixth key makes `read_scores` raise on the WHOLE file — which is why all 300 committed rows carry `accent_consistency` present-and-null today (the key IS on all 300 and null on all 300; a previous report got this backwards). Widen read_scores with a test named for the behaviour, keep `null` meaning "no bake existed" and never `pass`, and preserve the rule that scoring a clip nobody played is the one lie the sheet exists to prevent. The specific question cast.yaml's D-CAST-ES-02 asks the reviewer (Rosa against Plumas) must be answerable once a bank exists. (2) B3, REDRAW, AFTER THE PACK: today `coursekit sample es` refuses rather than drawing — "no exercise artefact at build/es/g7/exercises.jsonl … drawing from nothing would produce an empty sheet and a defect rate of 0%" — and that refusal is correct behaviour; keep it. The 4.00% figure (12/300 wrong, 68/300 awkward, reviewer_kind opus-agent-reviewer, seed 20260911, 58 strata) was measured over CANDIDATE and CORPUS populations, not over exercises, which is why the published `wrong_item_rate` is still `None` and `gate_passed()` is false. Once pipeline-g6-to-g9 produces build/es/g7/exercises.jsonl, draw the 300 fresh, re-score, and publish the rate. The gate is ≤2%; the route is measured, not hoped for: three repeated templates account for six of the twelve defects and the authoring lanes are fixing those texts, which lands exactly 6/300 = 2.00% — at the gate, not under it. Keep the verbatim `PROVISIONAL (unreviewed by a paid native speaker)` note; the plan's non-negotiable 5 is that the measured rate is shown to the learner, and a paid native pass is docs/RELEASE.md item 1, not this task. (3) Do NOT edit content/es/candidates/* (the three authoring lanes own those files) and do NOT add validators under tools/coursekit/src/coursekit/validators/ (the pipeline lane owns that directory) — the `cancha`/`pista` variety word-list that scores-method.md proposes as a G6 check gets filed in docs/P2-BLOCKERS.md, not implemented here. (4) Re-score honestly: this is an Opus agent scoring an Opus agent's Spanish and the report must say so in the same breath as the number.

### `p2r4/ci-reporter-timeout`

- lane: `.github/workflows/ci.yml`, `vitest.config.ts`, `vitest.workspace.ts`, `packages/core/src/day/recovery.test.ts`, `packages/core/src/day/recovery.*.test.ts`, `packages/testkit/src/`, `docs/ci.md`, `docs/owned/p2r4-ci-reporter-timeout.json`
- invariant ids: —
- depends_on: —

Fix B20, the infrastructure failure that has twice made a green gate unreadable. Symptom: `ci.yml` fails with `Error: [vitest-worker]: Timeout calling "onTaskUpdate"` while reporting `Test Files 114 passed | 1 skipped` and `Tests 1316 passed | 6 skipped`. It is the reporter's per-task RPC to the worker timing out, not a test. Observed on 86f2430 (both attempts, including a `gh run rerun --failed`) and on 682f7f2 (first attempt, cleared on a re-run), green on 859f3fb, 42b0be3, bbf3f05. THE MECHANISM IS MEASURED AND IT IS RUNNER SPEED, not the suite: ranking every attempt by duration, the failures are the slowest runs (86f2430 at 227.31 s wall / 604.56 s test-time; 682f7f2 at 212.36 s / 567.11 s) and the successes the fastest (42b0be3 at 152.73 s / 401.53 s), over an identical 1,316-test suite. That was written down as a prediction after the first two failures and 682f7f2 then confirmed it out of sample. TWO CONSEQUENCES YOU MUST DESIGN AROUND. First, a same-sha re-run is not a reliable clear: it failed twice on 86f2430 and cleared first try on 682f7f2, which is exactly what the "re-run once before calling it a regression" rule assumes and does not get. Second, and this is the honesty trap this project has already paid for once (the whimprflow "fixed it five times without fixing it" round): A GREEN RUN IS NOT EVIDENCE THAT YOUR FIX WORKED. Only a green run at 220 s+ wall counts, or a change that removes the reporter's per-task RPC from the path entirely and can be argued structurally. The suite is 27 s wall on this Mac against 227 s on the runner, so the timing cannot be reproduced locally — say so rather than claiming a local green proves anything. THE LEVERS, measured and unchanged: a quieter CI reporter (the default reporter's per-task RPC is what times out); `fileParallelism` so no single worker holds a 15–25-second property test while the reporter waits; or splitting packages/core/src/day/recovery.test.ts (four properties, 25 s together) out of the main run. NOT `PROPERTY_RUNS` — the ≥10,000-case floor is a gate on purpose (docs/ci.md §The property floor, plan §Verification, and the P0 gate reads 10,000 in EACH of four zones, not 2,500 each); lowering it to make the clock fit is the one fix that is forbidden. While you are in ci.yml, make it print wall and test-time on every run so the correlation stays measurable for whoever is next. Structural note for your report: every commit to docs/P2-BLOCKERS.md triggers ci.yml and adds an attempt to the denominator, so quote a floor and the mechanism, never an "N of M" rate — three previous revisions of that row were each invalidated by the push that carried them. One unreproduced neighbour, recorded and NOT to be chased here: of ~20 back-to-back local pytest runs, one died with `libc++abi: … recursive_mutex lock failed: Invalid argument`; every other run was 934 passed / 8 skipped, this Mac only, never in CI.


Owned ids for the phase gate (from the planner): INV-PACK-06, INV-PACK-07, INV-PACK-08, INV-PACK-10, INV-PACK-13, INV-PACK-14, INV-PACK-15, INV-PACK-17, INV-PACK-40, INV-PACK-41, INV-PACK-50, INV-PACK-51, INV-AUD-08

Gate (from the planner): P2 Spanish pack v0 (plan §Phases): `uv run coursekit validate es` green (exit 0); V1–V4 = 100% with zero violations [INV-PACK-06]; the baked bank within the 120 MB per-language budget on real bytes [INV-PACK-15]; every attribution-required sentence carries an owner string and is reachable from the credits surface [INV-PACK-17, V10/INV-PACK-13]; the INV-PACK-13 licence allow-list active from the first ingest; plus the commands the phase brief names — `coursekit build es` (G0–G9) exit 0, `coursekit sample es` drawing 300 items with a published wrong-item rate ≤2%, `coursekit sign es`, and the built pack opening in `packages/core`'s loader.
