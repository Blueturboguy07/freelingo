# P6 Stories + Radio

Source: the approved plan `~/.claude/plans/vectorized-coalescing-sun.md` (phase table row P6) and the P1–P8 workflow script `~/duolingo-research/build/p1-p8.workflow.js`. Needs a device for verification: **true**.

## Brief

Stories: format + player S101–S113 (reader with beats, SELECT_PHRASE, CONTINUATION, POINT_TO_PHRASE, ARRANGE, MULTIPLE_CHOICE, MATCH last, listening mode with gapless stitched audio and karaoke only on aligned assets, legendary story), stories/ module (INV-STO-01..06). Radio: format + player S114–S117 (episode model, transport, question cards that pause playback, transcript, useAudioPlaylist + setActiveForLockScreen doNotMix, the 3-minute background rule). coursekit: story and radio generators producing scripts through the same validators (V6 with per-story narrator register, INV-PACK-09/10) and the same cast voices; extend the Spanish pack with ≥6 stories and ≥3 radio episodes; audio budget accounted (INV-PACK-15). Tests: §15 STO, AUD-03/04; Maestro story + episode flows. Gate: green in CI with artefacts.
