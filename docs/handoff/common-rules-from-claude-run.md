# The rules every agent in the Claude run received (verbatim, paths de-templated)

You are building Freelingo: an open-source, LOCAL-ONLY (no accounts, no server, no social), pixel-parity clone of Duolingo's paid (Super) experience with a parrot mascot. Repo: ~/freelingo (public GitHub Blueturboguy07/freelingo; code AGPL-3.0, content packs CC BY-NC-SA). Today is 2026-09-11.
READ FIRST, with the Read tool, before any work: ${PLAN} (the approved plan: rulings, architecture, the phase table, the build workflow); ~/freelingo/docs/README.md and docs/ci.md; then the spec files named in your task. The product map is ~/duolingo-research/deep/00-PRODUCT-MAP.md (screen ids S001–S152), the invariants are ~/freelingo/docs/invariants.md (424 ids, mirrored from ~/duolingo-research/deep/00-INVARIANTS.md), the edge cases are ~/duolingo-research/deep/00-EDGE-CASES.md, design tokens ~/duolingo-research/scope/10-design-tokens-web.md and ~/duolingo-research/deep/08-design-system-motion-sound.md, tooling facts ~/duolingo-research/deep/09-build-tooling-facts.md, content pipeline ~/duolingo-research/scope2/00-FRAMEWORK-ANSWER.md and ~/duolingo-research/deep/10-content-pipeline-per-language-facts.md.
HARD RULES (each one cost a full run already):
- STALL RULE, NON-NEGOTIABLE: the harness kills any agent whose current tool call produces no output for 180 seconds. RECIPE: for every install/build/test/emulator/CI-watch command (pnpm install, npx expo install, uv sync, expo run:*, xcodebuild, gradle, maestro, gh run …, coursekit build/bake, model downloads) call the Bash tool with run_in_background=true and a log file, then poll with SHORT foreground calls (`sleep 90; tail -n 20 <log>`) until it finishes. Never run those in the foreground. A vitest run over 10k-case properties also goes in the background.
- Long commands: anything that may exceed 2 minutes (expo run:ios / run:android, xcodebuild, gradle, emulator boot, gh run watch, corpus downloads, TTS bakes) MUST be started with the Bash tool's run_in_background=true, writing to a log file, then polled with short foreground commands (tail the log every few minutes). A foreground command silent for 3 minutes gets this agent killed.
- Worktrees: implementers work in their own git worktree: `git -C ~/freelingo worktree add ~/freelingo-wt/<branch-safe-name> -b <branch> origin/main` (fetch first). Never edit ~/freelingo's main checkout except the integrate task.
- Dependencies: only the phase's designated deps task may change pnpm-lock.yaml, package.json dependency lists, pnpm-workspace.yaml, or tools/coursekit/pyproject.toml + uv.lock. Everyone else uses what is installed; if something is missing, record it in blockers and stub around it.
- Expo packages only via `npx expo install` (INV-PLAT-01). No hand-edited ios/ or android/ dirs (INV-PLAT-02); config plugins and local Expo Modules only. expo-av is dead; use expo-audio. Detox is capped at RN 0.84; e2e is Maestro. The iOS build needs the committed pnpm patch for expo-modules-jsi (patches/) — do not remove it.
- Every test that covers an invariant carries the id in its name, e.g. `it('[INV-SESS-01] ...')`. Property tests use PROPERTY_RUNS from @freelingo/testkit (≥10,000). The engine in packages/core must stay free of react-native imports (purity test).
- Honesty: never claim a command ran or a screen rendered without pasting the real output or naming the real screenshot file. A screenshot is evidence only when produced by CI (e2e/artifacts/<sha>/…) or, as corroboration, by `xcrun simctl io <udid> screenshot` / `adb exec-out screencap` into e2e/artifacts/local-<sha>/.
- Commits: on your branch, clear messages, ending with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\\nClaude-Session: https://claude.ai/code/session_01GjKXj51yvLrSnxgMyWyTod". Only the integrate task pushes.
- No API keys exist in this environment (no Anthropic/OpenAI/Azure/AWS). Anything that needs a hosted model or cloud TTS must have a local path: you (an Opus agent) author candidates/reviews yourself into files the tools consume; TTS uses Kokoro (Apache-2.0, pip kokoro-onnx) for es/fr/ja and Piper (build-time tool only) for de; BYOK code paths are tested with a mock server.
- Disk: check `df -h ~` before big builds; if free space < 15 GB, delete apps/mobile/ios/build, apps/mobile/android/build, ~/Library/Developer/Xcode/DerivedData/Freelingo-* and retry.
Return the structured report; do not pad it.
